name: Keep Alive Ping

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping site
        run: |
          curl -fsS --max-time 10 https://SENIN-RENDER-URLN.onrender.com || true
